# 애드센스 재심사 체크리스트

2026-08-03 반려("가치가 별로 없는 콘텐츠") 대응. **코드 수정 후 바로 재심사 누르지 말 것** — 구글이 새 페이지를 색인하기 전에 재심사하면 심사관은 반려 당시와 같은 사이트를 본다.

## 반려 원인 진단 (2026-08-03)

1. 색인 가능한 실질 콘텐츠가 6페이지뿐 (about/guide/examples + 블로그 3편)
2. 갤러리 상세 30장 전부 `noindex` — 사이트 콘텐츠의 80%가 심사 대상에서 제외돼 있었음
3. 갤러리 상세가 템플릿 문장 돌려쓰기 (고유 텍스트 400~600자)
4. 홈/guide/examples/블로그 간 예시·조언 문구 중복
5. UGC(제목·랭킹·댓글)가 해시 라우트라 색인 불가

## 적용한 수정 (재심사 전 배포 확인)

- [ ] 갤러리 30장 고유 해설(600~800자) + `index, follow` 전환 + sitemap 등록
- [ ] 페이지 간 중복 문구 제거 (소유권: 예시 제목→examples, 30초 루틴→guide)
- [ ] 블로그 3편 → 12편 (편당 1,500자+)
- [ ] `/titles/<imageKey>/` 서버렌더 페이지로 UGC 색인 가능화
- [ ] `node scripts/validate.mjs` 통과 후 main push → Pages 배포 확인

## 재심사 전 대기 절차 (2~4주)

1. **배포 직후:** [Google Search Console](https://search.google.com/search-console)에서
   - 새 `sitemap.xml` 재제출
   - 대표 URL 수동 색인 요청: `/gallery/` 상세 3~4개, 새 블로그 글 3~4개, `/titles/` 1~2개
2. **매주 확인:** GSC 색인 생성 → 페이지 → "색인 생성됨" 수가 늘어나는지. 목표: **갤러리 상세 + 블로그 신규 글의 절반 이상 색인**.
3. **색인 확인 방법:** `site:jemokhakwon.com` 검색 결과 수 변화.
4. **재심사 시점:** 다음 조건이 모두 충족될 때 (예상 2026-08-17 ~ 2026-08-31)
   - 새 페이지 20개 이상 "색인 생성됨"
   - `site:` 검색에 갤러리 상세·새 블로그 글이 보임
   - 사이트에 방문 트래픽이 조금이라도 쌓임 (심사에 유리)
5. 그때 애드센스 → "문제를 수정했음을 확인합니다" 체크 → 검토 요청.

## 재심사 반복 반려 시 다음 카드

- 갤러리에서 해설이 얇은 사진을 실제로 내리고 (purge 스크립트 + prod 정리 — 위험 작업 규율 준수) 밀도 높은 15~20장 체제로
- 블로그 주 1편 추가 발행 지속
- `/titles/` 페이지를 sitemap에 동적 포함 (제목 3개 이상인 사진만)

---

## 2차 반려 (2026-08-13) 기록 및 대응

**사유:** 동일 ("가치가 별로 없는 콘텐츠")

**원인 진단 (2026-08-16, GSC 확인):**
1. 색인 대기 없이 재심사 요청 — 자체 기준(색인 20개+, 8/17 이후)을 어김. 반려 시점 색인 페이지 **13개**뿐 (sitemap 51개 발견 대비). 심사관이 본 사이트는 1차 반려 때와 사실상 동일.
2. `product-week1-0956.pages.dev`가 애드센스에 별도 사이트로 등록돼 있고 "게시자 콘텐츠가 없는 화면에 Google 게재 광고" 정책 플래그 + ads.txt 없음 상태. 중복 도메인 + 계정 정책 플래그.

**적용한 조치:**
- [x] pages.dev 전체 → jemokhakwon.com 301 리다이렉트 (2026-08-16). `_redirects`는 호스트 매칭 미지원이라 `functions/_middleware.js` + `_routes.json` 확장으로 구현
- [ ] 애드센스 사이트 목록에서 `product-week1-0956.pages.dev`, `product-builder-lecture-4g2.pages.dev` 삭제 (수동)
- [x] GSC 수동 색인 요청 (2026-08-17, 총 9건 = 사용자 1 + 8):
  - 갤러리: imm-014, subject-7, subject-6, cat-smoke(noindex 캐시 7/14 → 재크롤 강제), subject-11, imm-012, imm-013, imm-015
  - 블로그 신규 글(animal/emotion/funny-photo-titles, observation-training)은 **이미 색인됨** 확인 → 요청 안 함
  - 남은 미색인 갤러리는 다음 날 이후 한도로 계속 (하루 ~10건)

**3차 재심사 조건 (전부 충족 전까지 버튼 금지):**
- GSC "색인 생성됨" **20개 이상** (현재 13, 매주 확인)
- `site:jemokhakwon.com`에 갤러리 상세·새 블로그 글 노출
- 2차 반려일로부터 **최소 2주 경과** (2026-08-27 이후)

## 3차 재심사 딥리서치 결론 (2026-08-22) — 권장 신청일 09-03 ~ 09-10

8/27은 최소 경과일일 뿐 신청일이 아니다. 핵심 수정(pages.dev 301, 8/16)의 재크롤이 그때까지 11일차라 심사관이 옛 캐시(noindex 갤러리·중복 도메인)를 볼 리스크가 남는다. 반복 반려에 공식 누적 페널티는 없지만(10회+ 반려 후 승인 사례 존재), 심사 1사이클이 2~4주라 성급한 실패의 기회비용이 한 달이다. 재신청 실전 컨센서스는 "최소 2주, 이상적 3~4주 + 수정이 색인에 반영된 것을 확인한 후".

**신청 전 최종 체크리스트 (하나라도 미달이면 1주 단위 연기):**

1. GSC "색인 생성됨" ≥ 20 (가능하면 25~30 — sitemap 51개의 절반)
2. `site:jemokhakwon.com`에 갤러리 상세 + 블로그 노출 확인
3. GSC URL 검사에서 주요 페이지 **마지막 크롤 날짜가 8/16 이후**인지 확인 (심사관이 볼 캐시가 갱신됐다는 직접 증거)
4. 애드센스 사이트 목록에서 `product-week1-0956.pages.dev`, `product-builder-lecture-4g2.pages.dev` 삭제 완료 (기존 미완 항목)
5. 2차 반려 이후 블로그 3~5편 신규 발행(1,000자+) — "반려 후 콘텐츠 추가" 신호
6. 필수 페이지 4종 점검: 개인정보처리방침·소개·문의·이용약관(운영정책에 UGC 검수 정책 한 단락 추가 권장)
7. (5분) 애드센스 지급 정보의 주소 확인 상태 점검 — 주소 미확인이 무사유 반려를 만든 사례 있음

**승인 후 UGC 정책 대비 (광고 게재 페이지의 사용자 콘텐츠는 퍼블리셔 책임 — support.google.com/adsense/answer/1355699):**
- 사용자 제목 금칙어(성인·혐오·괴롭힘) 필터 — 사전 또는 사후 검수
- 검수가 안 닿는 페이지에는 광고 코드를 넣지 않는 것도 안전한 옵션
- 갤러리 원본 이미지의 저작권/출처 정리 (UGC 사이트의 흔한 정책 지뢰)

참고: 트래픽은 공식 요건이 아니며 일 5~10명 실방문이면 충분하다는 소스 다수 — 현재 DAU(2~14)는 차단 사유 아님. 방문자 집계(daily_visits, 2026-08-22 가동)가 쌓이면 "실사용자 존재" 근거로도 활용.

## GSC "NOINDEX 새 이유" 메일 (2026-08-17 수신) — 조치 불필요 판정

- 메일의 NOINDEX 6건 정체: imm-024(의도된 noindex) + photo-007·photo-003·cat-smoke·subject-2·subject-12(7/14~7/24 옛 크롤 캐시, **라이브는 전부 index,follow 확인**)
- 스테일 5건 중 cat-smoke는 8/17에, 나머지 4건은 8/18에 수동 색인 요청 완료 → 재크롤되면 이 카테고리는 imm-024~027만 남는 게 정상
- 좋은 신호: "발견됨 - 현재 색인 생성되지 않음" 0 → 23 (사이트맵 URL들이 크롤 대기열에 들어옴)

---

## 3차 반려 (2026-09-01) 기록 및 대응

**사유:** 동일 ("가치가 별로 없는 콘텐츠"). 메일은 정형문이고 구체 사유는 콘솔 상태 세부정보에만 표시.

### 실측 진단 (2026-09-01)

반려 시점 사이트 상태를 문장 단위로 측정한 결과:

| 항목 | 실측 |
|---|---|
| 색인 대상 페이지 | 정적 7 + 블로그 12 + 갤러리 상세 33 = 약 52 |
| 갤러리 상세가 차지하는 비중 | **63%** |
| 갤러리 상세 페이지당 본문 | 총 1,370자 / **고유 905자** |
| 38장 전부에 동일한 문장 | 8줄 (약 470자) |
| 해설 원고가 없어 `noindex`인 갤러리 | 5장 (imm-024·025·026·027·031) |
| `/titles/` UGC 페이지 | **35개가 `index,follow`인데 sitemap에 0개** |

핵심 원인 4가지:

1. **같은 사진에 대한 얇은 페이지 쌍 35개.** `/gallery/<slug>/`(해설)와 `/titles/<key>/`(제목 랭킹)가 같은 사진·같은 `description` 메타를 쓰는 별도 URL이었다. 둘 다 얇고 서로 중복.
2. **사이트에서 가장 고유한 콘텐츠(실사용자 제목 1,200건+)가 발견 경로에 없었다.** `/titles/` 35개가 sitemap 54개 항목에 단 하나도 없었음.
3. **갤러리 상세의 공통 골격 비중이 34%** (1,370자 중 470자). 섹션 제목·연결 문구·출처 3문단·CTA가 전 페이지 동일.
4. **홈(`/`) 원본 HTML이 빈 앱 껍데기.** 기본 뷰 `#galleryGrid`가 빈 div, 실제 콘텐츠는 전부 JS 렌더.

블로그 12편(편당 고유 2,000자, 목차 구조 전부 상이)과 트래픽(28일 300클릭)은 문제 없음으로 확인.

### 적용한 조치 (2026-09-01)

- [x] **한 사진 = 한 페이지로 통합.** `functions/gallery/[slug].js`가 해설 + 실제 사용자 제목 랭킹(하트·댓글 수 포함)을 함께 서버렌더. `/titles/<key>/`는 쿼리 보존 301. 정적 상세 HTML 38개 삭제. → 중복 쌍 35개 소멸, 페이지당 고유 콘텐츠가 해설 900~1,200자 + UGC로 증가 (사진 38장 중 30장이 유저 제목 20~50개 보유).
- [x] **공통 골격 470자 → 110자로 축소.** 로테이션 연결 문구·출처 3문단·중복 안내문 제거, 섹션 제목과 CTA만 공통. 페이지 평균 고유 자수 905 → 1,028자.
- [x] **미작성 5장 해설 집필** (imm-024·025·026·027·031, 편당 1,100자+) → `noindex` 해제, sitemap 등록. sitemap 54 → **59 URL**.
- [x] **UGC가 적어 얇게 남던 3장 심화** (imm-028 681→1,154자, imm-029 670→1,120자, imm-030 740→1,116자).
- [x] **홈 서버렌더.** `functions/index.js`가 정적 `index.html`의 빈 `#galleryGrid`에 사진 38장 카드와 상세 링크를 주입. 원본 HTML 보이는 본문 약 1,864자 + **상세 페이지 내부링크 38개**. main.js가 `replaceChildren()`으로 다시 그리므로 사용자 화면은 동일(점진적 향상). 실패 시 원본 자산 그대로 반환.
  - **주의:** `#homeView`(피드)는 정적 HTML에서 `hidden`이라 주입 대상으로 쓰면 안 된다(은닉 텍스트로 보일 수 있음). 실제 기본 표시 뷰는 `#galleryView`.
- [x] `node scripts/validate.mjs` ALL PASS, 로컬 `wrangler pages dev`에서 38장 전부 200 + `index,follow` 확인, 홈 콘솔 에러 0.

### 4차 재심사 조건 (전부 충족 전까지 버튼 금지)

이번에도 **배포 직후 재심사를 누르면 안 된다.** 구조가 바뀐 페이지가 재크롤되기 전에는 심사관이 반려 당시 캐시를 본다.

1. 배포 후 GSC에 **sitemap.xml 재제출**(59 URL), 그리고 `/gallery/` 상세 5~8개 + 홈 수동 색인 요청
2. `/titles/` → `/gallery/` 301이 GSC "리디렉션이 포함된 페이지"로 잡히는지 확인 (의도된 것이며 정상)
3. GSC "색인 생성됨" **25개 이상**, 그리고 URL 검사에서 주요 갤러리 상세의 **마지막 크롤 날짜가 2026-09-01 이후**
4. 3차 반려일로부터 **최소 3주 경과 → 2026-09-22 이후** (2차 때 조건 미달로 조기 신청해 실패한 전례 반복 금지)
5. 기존 미완 항목: 애드센스 사이트 목록에서 `product-week1-0956.pages.dev`, `product-builder-lecture-4g2.pages.dev` 삭제, 지급 주소 확인 상태 점검
