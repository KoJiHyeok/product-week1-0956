# "서버 저장소를 이용할 수 없습니다" 진단 & 수정 런북

작성: 2026-06-08 · 대상: product-week1-0956 (Cloudflare Pages + D1)

---

## ✅ 결과 (2026-06-08 해결됨)

- **원인 확정:** prod D1이 0011까지만 마이그레이션됨 → `submissions`에 `hidden_at/deleted_at/excluded_from_ranking` 없음 → 목록 쿼리 `no such column` → 500.
- **수정 완료:** `wrangler d1 migrations apply ... --remote`로 0012·0013·0014 적용. `table_info`에 컬럼 5개 확인됨. **DB 수정이라 코드 배포 없이 라이브 즉시 반영.**
- **남은 일:** 라이브 사이트에서 제목 등록 재현 테스트 → `.git/index.lock` 삭제 → `.gitattributes` 커밋. 진단용 `detail`은 역할 끝나 제거함.

## TL;DR (경위)

- 그 에러는 **R2/저장소 문제가 아님.** 로그인 사용자의 `/api/submissions` 요청이 어떤 이유로든 실패하면 프런트가 뭉뚱그려 띄우던 토스트였음.
- 코덱스의 코드 수정(토스트가 서버 메시지 표시 + 서버 `console.error` 로깅)은 **이미 HEAD에 커밋됨.**
- 내가 추가로 처리한 것: CRLF 노이즈 청소(`.gitattributes`), 진단용 `detail` 필드(원인 확정 후 제거).

---

## 1. 500의 진짜 원인 후보 (둘 중 하나)

`functions/api/submissions/index.js`의 `try/catch`를 보면 같은 500이 두 갈래에서 남:

1. **D1 바인딩 누락** — `getDb()`는 `context.env.DB`가 없으면 `"D1 DB binding is not configured"`를 throw.
   → Pages **프로덕션 환경에 D1 바인딩이 안 붙어있는** 경우.
2. **마이그레이션 미적용** — 목록 조회 쿼리가 `hidden_at / deleted_at / excluded_from_ranking`(마이그레이션 `0013`에서 추가)을 참조함. prod DB에 그 컬럼이 없으면 `no such column`을 throw.

둘 다 사용자에겐 똑같은 일반 메시지로 가려져서 토스트만으론 구분 불가. **A 또는 D로 실제 에러 문구를 봐야 구분됨.**

---

## 2. 내가 한 변경 (검증 완료)

### C. CRLF 줄바꿈 노이즈 청소 — `.gitattributes` 추가
- 증상: 워크트리 94개 파일이 전부 "modified"로 떴는데, 내용 변경 0 · 순수 CRLF(LF→CRLF) 노이즈였음. (Windows 에디터/OneDrive가 줄바꿈을 바꿈)
- 위험: 이대로 `git add .` 하면 16,000줄짜리 쓰레기 커밋 → 진짜 diff가 안 보이고 merge 지옥.
- 처리: `.gitattributes`에 `* text=auto eol=lf` 추가. git이 diff 비교 시 CRLF를 LF로 정규화 → 93개 노이즈가 즉시 사라짐. (`main.js`만 남는데 그건 **너의 실제 갤러리 작업**이라 안 건드림)

### D. `submissions` 500 응답에 진단용 `detail` 추가
- `functions/api/submissions/index.js`의 list/create `catch`에 `detail: error?.message` 추가.
- 프런트 토스트는 `message`만 쓰므로 **사용자 화면은 그대로 깔끔**, 실제 원인은 **DevTools → Network → 실패한 `/api/submissions` 요청 → Response**의 `detail`에서 확인 가능.
- ⚠️ 임시 필드. **원인 확정 후 제거하거나 디버그 게이팅** 할 것(코드에 주석 남김).

---

## 3. 네가 직접 할 일 (순서대로)

### 0) ⚠️ `.git/index.lock` 먼저 삭제 — 내가 만든 부작용
내 환경에서 `git checkout`이 OneDrive 마운트의 unlink 권한 때문에 실패하며 **stale lock 파일을 남겼음.** 이게 있으면 다음 `git add`/`commit`이 막힘. 실기기에서:

```powershell
# PowerShell
Remove-Item ".git\index.lock"
# 또는 cmd:  del .git\index.lock
```
(0바이트 stale lock이라 삭제 안전. 진행 중인 트랜잭션 없음)

### A) 원인 확인 — read-only, prod 안 건드림 ✅ 먼저 이것부터
`CLOUDFLARE_API_TOKEN` 세팅하거나 `npx wrangler login` 후:

```bash
# 미적용 마이그레이션이 있는지 (0013/0014가 pending이면 원인 확정)
npx wrangler d1 migrations list product-week1-0956-auth --remote

# submissions 테이블에 새 컬럼이 실제로 있는지 (PowerShell: 한 줄로)
npx wrangler d1 execute product-week1-0956-auth --remote --command "PRAGMA table_info(submissions);"
```
→ `hidden_at`이 안 보이거나 0013이 pending이면 **원인 = 마이그레이션 미적용.**

동시에 **Cloudflare 대시보드 → Pages → 프로젝트 → Settings → Functions → D1 database bindings**에서 `DB`가 Production에 붙어있는지 눈으로 확인(후보 1번 배제용).

### B) 수정 — A에서 마이그레이션 미적용으로 확정됐을 때만
```bash
npx wrangler d1 migrations apply product-week1-0956-auth --remote
```
**전체 영향 (먼저 그려보기):** pending 마이그레이션이 전부 적용됨.
- `0013`: `submissions`/`comments`에 컬럼 5개씩 `ADD COLUMN` + 인덱스 2개 생성
- `0014`: `admin_daily_summaries` 테이블 생성

전부 `ADD COLUMN` / `CREATE INDEX IF NOT EXISTS` → **추가형·비파괴적**(기존 데이터 안 지움). 그래도 prod DB라 `migrations list`로 적용 대상 확인 후 실행할 것.

### (대안) 토큰 없이 원인 보기 — D 활용
A의 토큰이 없으면: 아래 4)로 커밋·푸시 → Cloudflare Pages 자동 빌드 → 사이트에서 제목 등록 재현 → **DevTools Network 응답의 `detail`** 읽기. `"D1 DB binding is not configured"`면 후보 1번, `"no such column: hidden_at"`면 후보 2번.

---

## 4. 깔끔하게 커밋하기 (CRLF 노이즈 없이)

`index.lock` 삭제 후:

```bash
git status              # main.js(=네 갤러리 작업), submissions/index.js(=detail), .gitattributes 만 보여야 정상
git add .gitattributes functions/api/submissions/index.js
git commit -m "Add .gitattributes (LF) and expose submission error detail for diagnosis"
# 갤러리 작업도 올릴 거면:
git add main.js && git commit -m "Update gallery image titles"
git push                # → Cloudflare Pages 자동 배포
```

## 5. 원인 확정·수정 후 마무리
- `submissions/index.js`의 `detail: error?.message` 제거(또는 `?debug=1`류로 게이팅). raw 에러를 prod에서 계속 노출하는 건 사소하지만 정보 누출.
- 같은 패턴(원인 미상 500 → 토스트만 보고 헤맴)을 막고 싶으면, 코덱스가 로깅만 넣은 다른 API(하트·댓글·수정/삭제)에도 같은 `detail` 또는 에러코드를 1건씩 적용 → 확인 → 전체 적용.
